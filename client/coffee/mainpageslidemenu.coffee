

# TemplatedaysList..days.active = ->
# 	if (Session.equals "selectedDayInSlideMenu", @_id then "active" else "")

# Template.daysList.events
# 	"click .dayOfWeek": ->
# 		alert "Yeah!"

# Template.news.active = () ->
# 	(if Session.equals("news_selected", @_id) then "active" else "")


